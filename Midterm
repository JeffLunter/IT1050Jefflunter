using System;

namespace Midterm
{
    class Program
    {
        static void Main(string[] args)
        {
            /* 1. The three type of control structures are the selection, iteration and jump. Selection statements consist if and else, the iteration statements consist of do anf for, anf the jump statement consist
             * of break and goto.
            
              2.
            int i = 12; 

            Boolean keepLooping = true;

            while (keepLooping)
                { if (i <= 11)
                    keepLooping = false;
                i++;

                Console.WriteLine(i);
                }   

            3.

            int i = 2;
            while (i <+ 128)
            {
                Console.WriteLine(i);
                i *= 2;
            }

            4. 
            int i;

            for (i = 49; i >= 1; i--)
            {
                Console.Write(i + ",");
            }

            5.
            int i = 1;
            while (i <= 21)
            {
                Console.Write(i + " ");
                i+=2;
            }    

            6.  The code will put out a single *. As a while statement, the code will put out no stars.
            int n = 8;
            int i = 10; // initialize
            while (i < n)
            {
                Console.Write("*");
                i++; // update!
            } ;
            */
        }
    }
    }
